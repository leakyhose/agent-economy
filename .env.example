# Copy to .env and fill in.  cp .env.example .env
# .env is gitignored and overrides anything exported in your shell.

# ---- which brain drives the agents: stub | openai | claude
BRAIN=openai

# ---- API keys (only the one for your brain is needed)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ---- model (defaults: gpt-5.6-luna for openai, claude-haiku-4-5 for claude)
MODEL=gpt-5.6-luna

# ---- simulation size and speed
AGENTS=10
# TICK_MS=500            # length of one game tick in ms
# ROUND_TICKS=6          # market clears every N ticks
# EAT_TICKS=8            # each agent eats 1 food every N ticks
# STAGGER_MS=6000        # agents wake up spread over this window
# START_CASH=5000        # starting coins per agent, in cents
# START_FOOD=6
# LLM_CONCURRENCY=12     # max simultaneous model calls

# ---- server / chain
# PORT=8787
# RPC=http://127.0.0.1:8899
# RUN_SECONDS=0          # >0 = run headless for N seconds, print a summary, exit
