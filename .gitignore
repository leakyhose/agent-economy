node_modules/
.next/
dist/
.env
.env.local
*.log
.DS_Store
test-ledger/
reference/
pnpm-lock.yaml
package-lock.json
runs/

# Build output.
target/
.next-build/

# Program keypairs are never committed. They are localnet throwaways, but a repo
# that contains no key material of any kind needs no one to judge which kind.
# Regenerating them changes each program id: see programs/README.md.
programs/*/keys/
*-keypair.json
